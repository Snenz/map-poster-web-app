import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "My Map Poster",
  description: "Create your own map poster or use existing designs from other users.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased p-5`}
        style={{ backgroundImage: "url('/icon_background.svg')", backgroundAttachment: "fixed" }}
      >
        {children}
      </body>
    </html>
  );
}
